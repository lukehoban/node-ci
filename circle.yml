machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install docker-compose

test:
  override:
    - docker-compose run web '/src/node_modules/.bin/mocha'

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY